[common]
bind_port = 7000
token = your-frp-secret-token

# Dashboard 监控（可选）
dashboard_port = 7500
dashboard_user = admin
dashboard_pwd = admin123

# 允许端口范围（stcp 不需要，但visitor需要）
allow_ports = 30000-39999