[Unit]
Description=FRP Visitor Client for GPU container %i
After=network.target

[Service]
Type=simple
User=root
Restart=always
RestartSec=2
ExecStart=/usr/local/bin/frpc -c /etc/frp/visitors/%i.ini
ExecReload=/bin/kill -HUP $MAINPID

[Install]
WantedBy=multi-user.target
