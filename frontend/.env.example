# Leave empty when the frontend and backend share the same origin or when Vite proxy is used.
VITE_API_BASE_URL=

# Used only by `npm run dev` to proxy /api requests.
VITE_PROXY_API_TARGET=http://127.0.0.1:9999
